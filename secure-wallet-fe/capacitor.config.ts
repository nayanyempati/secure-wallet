import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'secure-wallet-fe',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
